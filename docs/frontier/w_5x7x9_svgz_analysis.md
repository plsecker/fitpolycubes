# W 5×7×9: Published Solution SVGZ Analysis

**Date:** 2026-08-21
**Status:** SOLUTION ACQUIRED — 63-piece W tiling successfully extracted from published SVGZ

---

## Summary

The W 5×7×9 solution published on Shirakawa's site was successfully extracted from its SVGZ format. The SVGZ contains a color-coded cell grid with piece-number annotations, which was machine-parsed into 63 concrete W pentacube placements.

---

## Part 1: Source

**URL:** `https://puzzlewillbeplayed.com/Shirakawa/svgz/W/W-5x7x9.svgz`

The file is gzip-compressed SVG (3,129 bytes compressed, 32,535 bytes decompressed). The SVG was generated by "SVG creator for Polyomino Packing" by ISHINO k16.

---

## Part 2: SVG Structure

The SVG contains:
- **Title:** `W x 63 : 5x7x9`
- **Data:** 315 `<rect>` elements, each 10×10 pixels, color-coded by piece
- **Annotations:** Each `<rect/>` is followed by an XML comment identifying the piece number: `<!-- N -->`
- **Layout:** 9 rows (z-layers 0-8), each containing 35 cells arranged as 5 columns × 7 rows of the 5×7 grid

**Grid mapping:**
- SVG pixel (px, py) maps to box (bx, by, bz):
  - gx = px // 10, gy = py // 10 (grid position)
  - bx = gx // 8, by = gx % 8, bz = gy

---

## Part 3: Extraction Method

The extraction uses a simple regex on the raw SVG text:
```python
pattern = r'<rect\s+x="(\d+)"\s+y="(\d+)"\s+width="\d+"\s+height="\d+"\s+fill="([^"]+)"\s*/>\s*<!--\s*(\d+)\s*-->'
```

This extracts all 315 cell rectangles with their piece numbers. The cells are then grouped by piece number (1-63) to form 63 W placements.

---

## Part 4: Validation Results

| Check | Result |
|-------|--------|
| W pieces | 63/63 |
| Cells per piece | 5/5 (all) |
| Total cells | 315 |
| Cells in bounds (5×7×9) | 315/315 |
| Overlaps | None |
| Gaps | None |
| Legal W orientation | All 63 pieces valid |

**All checks pass. The solution is valid.**

---

## Part 5: Solution Location

The validated solution is saved at:
`data/solutions_parsed_w_5x7x9.dat`

Format matches the project's standard solution format (header, solution number, concatenated piece placements).

---

## Part 6: Next Step — Second-Orbit Search

With a validated first solution now available, the next step would be:
1. Determine the correct symmetry convention for W
2. Generate the first solution's full symmetry orbit
3. Search for any valid W tiling outside that orbit

---

**Report prepared by:** OpenWork automated analysis
**Date:** 2026-08-21
**Status:** FINAL (solution acquired)